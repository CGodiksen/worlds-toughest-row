[package]
name = "storage-sqlite"
version = "0.1.0"
edition = "2024"

[dependencies]
api-types = { path = "../api-types" }
storage = { path = "../storage" }
anyhow = "1"
rusqlite = { version = "0.40.1", features = ["bundled"] }
